import { Typography } from "@mui/material";

const ChatComponent = (props) => {
  return (
    <>
      <Typography variant="h4">This is chat component</Typography>
    </>
  );
};

export default ChatComponent;
